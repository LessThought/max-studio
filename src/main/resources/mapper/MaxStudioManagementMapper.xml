<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.screen.mapper.MaxStudioManagementMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.screen.pojo.MaxStudioManagement">
        <id column="id" property="id" />
        <result column="screen_name" property="screenName" />
        <result column="share_id" property="shareId" />
        <result column="screen_url" property="screenUrl" />
        <result column="screen_share" property="screenShare" />
        <result column="is_deleted" property="isDeleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, screen_name, share_id, screen_url, screen_share, is_deleted, create_time, update_time
    </sql>

    <insert id="saveScreen" parameterType="com.screen.pojo.MaxStudioManagement">
        insert into max_studio_management
        (id, screen_name, share_id, screen_url, screen_share, is_deleted, create_time, update_time)
        values (#{id}, #{screen_name}, #{share_id}, #{screen_url}, #{screen_share}, #{is_deleted}, #{create_time}, #{update_time})
    </insert>

</mapper>
